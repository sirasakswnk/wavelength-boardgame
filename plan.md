# Wavelength Online — แผนการพัฒนา (plan.md)

> เว็บเกม **Wavelength (คลื่นความถี่อ่านใจ)** แบบออนไลน์ real-time เล่นกับเพื่อนต่างสถานที่
> เอกสารนี้เป็น *source of truth* ของโปรเจกต์ — อัปเดตทุกครั้งที่มีการตัดสินใจใหม่

อัปเดตล่าสุด: 2026-08-11

**ชื่อเว็บ:** WaveLengthBoardGame

---

## 1. แนวคิดเกม (Game Concept)

> **โหมด: 2 คนแข่งกัน (1v1) — สลับกันทายคนละรอบ ใครทายแม่นกว่าแต้มรวมสูงกว่า**

- มี **วงล้อสเปกตรัม (spectrum dial)** ที่ซ่อน **ตำแหน่งเป้าหมาย (target)** ไว้
- แต่ละรอบมี **การ์ดหัวข้อตรงข้าม 2 ฝั่ง** เช่น "ร้อน ↔ เย็น", "เชย ↔ ล้ำสมัย"
- **คนใบ้ (Psychic)** ทำพิธี 3 สเต็ป: **แตะวงล้อหมุนสุ่มตำแหน่ง** (อนิเมชันเข็มทอง) → **แตะอีกครั้งเปิดฝาดูโซนเป้าหมาย** → **พิมพ์คำใบ้เอง** (คนทายถึงจะเริ่มหมุนได้)
- **คนทาย (Guesser)** อ่านคำใบ้แล้ว **หมุนเข็ม** ไปยังตำแหน่งที่คิดว่าใช่
- **เฉลย** → ให้คะแนนตามความใกล้ของเข็มกับเป้าหมาย → **เฉพาะคนทายได้แต้มของรอบนั้น** (เก็บคนละช่อง)
- **สลับบทบาท** คนใบ้ ↔ คนทาย เล่นรอบต่อไป

**เสน่ห์ของเกม:** การอ่านใจกันและถกกันว่าคำใบ้หมายถึงตรงไหนของสเปกตรัม

---

## 2. การตัดสินใจที่สรุปแล้ว (Decisions Locked)

| หัวข้อ | ตัวเลือกที่เลือก | หมายเหตุ |
|--------|------------------|----------|
| **Tech Stack / Backend** | **Firebase** (Realtime Database + Auth) | ซ่อนเป้าหมายด้วยการเก็บไว้คนละ path + Security Rules ให้อ่านได้เฉพาะคนใบ้ · ไม่ต้องใช้ Cloud Functions (อยู่บนแพลนฟรี Spark ได้) |
| **ขอบเขต MVP** | **แกนหลักพอเล่นได้** | สร้าง/เข้าห้อง · วงล้อ sync · ซ่อน/เฉลยเป้าหมาย · คิดคะแนน · สลับตา |
| **ชุดการ์ดหัวข้อ** | **ให้ผู้ช่วยร่างชุดภาษาไทยให้** | เริ่ม ~50–100 คู่ แบ่งหมวด |
| **Frontend** | React + Vite + TypeScript + SVG dial | วงล้อวาดด้วย SVG ให้หมุนลื่น |
| **Deploy** | หน้าเว็บ: Firebase Hosting (หรือ Vercel) · หลังบ้าน: Firebase | ฟรีทีเออร์ (Spark) เพียงพอ ไม่ต้องเช่า server เอง |
| **ชื่อเว็บ** | **WaveLengthBoardGame** | — |
| **ธีมสี** | **สว่างสดใส อ้างอิงอุปกรณ์ Wavelength จริง** | พื้นอควา + แบรนด์ teal×pink · หน้าปัดครีม+กรอบเทอร์ควอยซ์ · โซนคะแนน น้ำเงิน(2)/เหลือง(3)/ส้ม(4) · เข็มแดง · มี "ฝาปิด" เทอร์ควอยซ์ซ่อนเป้าหมาย · ธีมสว่างอย่างเดียว (ไม่มี dark mode) |
| **โหมดผู้เล่น** | **2 คนแข่งกัน (1v1) · คะแนนแยกช่องรายคน** | คนใบ้ **พิมพ์คำใบ้เอง** · สลับบทบาททุกรอบ · **เฉพาะคนทายได้แต้ม** ของรอบนั้น |
| **ภาษา UI** | **ไทยล้วน** | — |

---

## 3. โจทย์เทคนิคสำคัญที่สุด 🔑

> **ตำแหน่งเป้าหมายต้องซ่อนจากผู้เล่นคนอื่น** จนกว่าจะเฉลย
> ถ้าเก็บไว้ฝั่ง browser → เปิด DevTools แอบดูได้ ดังนั้นต้อง **server-authoritative**

### วิธีแก้บน Firebase: แยกข้อมูลเป็น 2 path + Security Rules

> Firebase ซ่อน "รายฟิลด์" ในเอกสารเดียวไม่ได้ → แต่ซ่อน "คนละ path" ได้ด้วย Security Rules

```
rooms/{code}/
├─ public/            ← ทุกคนในห้องอ่านได้ (sync realtime)
│    เฟสเกม · การ์ดหัวข้อ · คำใบ้ · ตำแหน่งเข็มที่ทาย · คะแนน · psychicUid
└─ secret/            ← 🔒 อ่านได้เฉพาะคนใบ้ (กันด้วย Rules)
     targetPosition
```

กติกาความปลอดภัย (Security Rules):
1. `secret` มี rule: `".read": "auth.uid === root.child('rooms/'+$code+'/public/psychicUid').val()"` → คนทายอ่านไม่ได้เลย (listener จะโดนปฏิเสธ)
2. เขียน `secret` ได้เฉพาะคนใบ้ของรอบนั้น
3. **คนใบ้เป็นคนสุ่ม target แล้วเขียนลง `secret`** — client คนอื่นอ่านไม่ได้ (โหมด co-op 2 คน เชื่อใจกันได้ ไม่ต้องมี server สุ่ม)
4. ตอนเฉลย: คนใบ้ (หรือคนทายหลังกดเฉลย) คัดลอก `targetPosition` ไปไว้ที่ `public` เพื่อโชว์ + คิดคะแนนฝั่ง client
5. *(อนาคต ถ้าอยากกันโกงเข้ม → ขึ้นแพลน Blaze แล้วย้ายสุ่ม/คิดคะแนนไป Cloud Functions)*

---

## 4. กลยุทธ์ Real-time (Firebase Realtime Database)

ใช้ **Realtime Database (RTDB)** เป็นตัวหลัก — เหมาะกับเกม real-time ที่เขียนถี่ๆ:

| งาน | วิธี | เหตุผล |
|-----|------|--------|
| **ลากเข็มสดๆ** (อัปเดตถี่มาก) | เขียน `public/needle` แบบ throttle (~ทุก 50ms) | RTDB ออกแบบมาเพื่องานเขียนถี่ latency ต่ำ |
| เปลี่ยนเฟส / คำใบ้ / คะแนน | เขียน/`onValue` ที่ `public/*` | ทุก client subscribe ที่ path เดียว sync ตาม |
| ใครออนไลน์อยู่บ้าง | `presence/*` + `onDisconnect()` | RTDB จัดการ auto-remove ตอนเน็ตหลุดให้ |

Flow เข็ม: คนทายลาก → เขียน `public/needle` (throttle) → คนใบ้ `onValue` เห็นสด → กด "เฉลย" ค่อย finalize + คิดคะแนน

---

## 5. Flow การเล่น (เฟส)

| เฟส | สิ่งที่เกิดขึ้น | action (Firebase) |
|-----|--------------|-----------|
| **Lobby** | สร้างห้อง → ได้รหัส 4 ตัว → เพื่อนกรอกรหัสเข้ามา | เขียน `rooms/{code}` + join ที่ `presence` |
| **แจกโจทย์** | สุ่มการ์ดหัวข้อ + คนใบ้สุ่มเป้าหมายลับ | เขียน `public/card` + `secret/targetPosition` |
| **ใบ้** | **คนใบ้พิมพ์คำใบ้เอง** → คนทายถึงเริ่มหมุนได้ | อ่าน `secret` (เฉพาะคนใบ้) → เขียน `public/clue` |
| **หมุน** | คนทายลากเข็ม (คนใบ้เห็น sync สด) | เขียน `public/needle` (throttle) |
| **เฉลย** | เปิดเป้าหมาย → คิดคะแนนตามความใกล้ | คัดลอก target → `public` + คิดคะแนน client |
| **สลับบทบาท** | คนใบ้ ↔ คนทาย เล่นรอบใหม่ | สลับ `public/psychicUid` + เริ่มรอบใหม่ |

**การคิดคะแนน (เริ่มต้น):** โซนกลาง (ตรงเป้า) = 4 · ถัดออกมา = 3 · รอบนอก = 2 · พลาด = 0

**การเก็บคะแนน:** เก็บ **แยกช่องรายคน** (`players/{uid}/score`) — **เฉพาะ "คนทาย" ได้แต้มของรอบนั้น** (วัดว่าใครอ่านคำใบ้แม่นกว่า) พอสลับกันทายครบ ก็เทียบว่าใครแต้มรวมสูงกว่า = แนวแข่ง 1v1

---

## 6. โครงข้อมูล RTDB (ร่าง)

```
rooms/{code}/
├─ meta/            phase · round · createdAt · hostUid
├─ players/{uid}/   name · avatar · score(แยกช่องรายคน)
├─ public/          psychicUid · cardId · cardLeft · cardRight
│                   clue(คนใบ้พิมพ์เอง) · needle(0..1) · revealed · roundScore
├─ secret/          targetPosition(0..1)          🔒 อ่านได้เฉพาะ psychicUid
└─ presence/{uid}/  online (+ onDisconnect ล้างอัตโนมัติ)
```

**Auth:** ใช้ Firebase **Anonymous Auth** ให้ผู้เล่นแต่ละคนมี `uid` คงที่ (จำเป็นสำหรับ Rules ระบุว่าใครเป็นคนใบ้)

**Security Rules (แก่น):** `secret` อ่าน/เขียนได้เฉพาะ `auth.uid === public/psychicUid` · `public` อ่าน/เขียนได้เฉพาะผู้เล่นในห้อง

---

## 7. โครงสร้างโปรเจกต์ (ที่จะสร้าง)

```
wavelength/
├─ src/
│  ├─ components/   Dial (SVG) · Lobby · Scoreboard · ClueBubble · PlayerCard · Controls · Confetti
│  ├─ hooks/        useRoom · useRealtimeNeedle · usePresence
│  ├─ lib/          firebase client · geometry · scoring
│  └─ data/         cards_th.ts (ชุดการ์ด)
├─ firebase/        database.rules.json (Security Rules) · firebase.json
├─ plan.md
└─ ...
```

---

## 8. ชุดการ์ดหัวข้อภาษาไทย (ร่างเริ่มต้น)

> เป้าหมาย 50–100 คู่ แบ่งหมวด — ด้านล่างคือชุดตั้งต้น ค่อยขยาย

### หมวดทั่วไป
| ซ้าย ◀ | ▶ ขวา |
|--------|-------|
| เย็น | ร้อน |
| ธรรมดา | พิเศษสุด |
| เงียบ | เสียงดัง |
| เรียบง่าย | หรูหรา |
| น่าเบื่อ | ตื่นเต้น |
| ไร้ประโยชน์ | มีประโยชน์ |
| ปลอดภัย | อันตราย |
| โบราณ | ทันสมัย |
| เก็บตัว | เข้าสังคมจัด |
| เรื่องเล็ก | เรื่องใหญ่ |

### หมวดกิน
| ซ้าย ◀ | ▶ ขวา |
|--------|-------|
| เผ็ดน้อย | เผ็ดจัด |
| อาหารคาว | อาหารหวาน |
| ของกินเล่น | ของกินจริงจัง |
| ของถูก | ของแพง |
| กินตอนเช้า | กินตอนดึก |

### หมวดป๊อปคัลเจอร์ / สไตล์
| ซ้าย ◀ | ▶ ขวา |
|--------|-------|
| เชย | ล้ำสมัย |
| น่ากลัว | น่ารัก |
| ซุปเปอร์ฮีโร่อ่อนแอ | ทรงพลัง |
| หนังสำหรับเด็ก | หนังสำหรับผู้ใหญ่ |
| ทักษะไร้ค่า | ทักษะมีค่า |

### หมวดกวนๆ / ชวนถก
| ซ้าย ◀ | ▶ ขวา |
|--------|-------|
| งานที่ไม่มีใครอยากทำ | งานที่ใครๆก็อยากทำ |
| ความลับที่บอกได้ | ความลับที่ห้ามบอก |
| เรื่องที่พูดในที่ทำงานได้ | เรื่องที่พูดไม่ได้ |
| โชคร้าย | โชคดี |
| น่าไว้ใจ | น่าสงสัย |

---

## 9. Roadmap การลงมือ

- [x] **ขั้น 1 — วางโครงโปรเจกต์** (Vite + React + TypeScript) ✅ build ผ่าน
- [x] **ขั้น 2 — วงล้อ SVG + logic คะแนน** (เล่น hotseat ในเครื่องได้แล้ว) ✅
- [ ] **ขั้น 3 — ต่อ Firebase** (RTDB + Auth + Security Rules) ให้เล่นข้ามเครื่องได้จริง
- [ ] **ขั้น 4 — ขยายชุดการ์ด + เก็บรายละเอียด UI/ธีม**

**วิธีรัน:** `npm install` → `npm run dev` (เปิด http://localhost:5173) · `npm run build` เพื่อ typecheck+build
**โครงไฟล์ที่ทำแล้ว:** `src/lib/{geometry,scoring}.ts` · `src/data/cards.ts` · `src/components/{Dial,ConceptCard,PlayerCard,ClueArea,Controls,Confetti}.tsx` · `src/App.tsx` · `src/types.ts`

### ฟีเจอร์เพิ่มทีหลัง (นอก MVP)
- แชตในเกม
- รองรับหลายคน / หลายทีมแข่งกัน (+ รอบโหวต "ซ้าย/ขวา" ตามกติกาต้นฉบับ)
- ตารางอันดับ / บันทึกสถิติ
- Reconnect ตอนเน็ตหลุด
- เสียง / อนิเมชัน เพิ่มความสนุก

---

## 10. คำถาม/สิ่งที่ยังไม่สรุป (Open Questions)

- ✅ ชื่อเว็บ: **WaveLengthBoardGame**
- ✅ ธีมสี: **สดใส สไตล์โต๊ะบอร์ดเกม**
- ✅ โหมด: **2 คน ทีมเดียว (co-op)**
- ✅ ภาษา UI: **ไทยล้วน**

*(ทุกข้อสรุปแล้ว — พร้อมลงมือขั้น 1–2)*
